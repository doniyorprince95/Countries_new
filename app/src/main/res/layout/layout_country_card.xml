<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tool="http://schemas.android.com/tools">

    <android.support.v7.widget.CardView
        android:id="@+id/card_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:layout_margin="@dimen/card_margin"
        card_view:cardCornerRadius="@dimen/card_view_radius"
        card_view:cardElevation="@dimen/card_view_elevation">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/imageView_countryFlag"
                android:layout_width="match_parent"
                android:layout_height="@dimen/flag_height"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:clickable="true"
                android:scaleType="centerCrop" />

            <TextView
                android:id="@+id/tv_countryName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@id/imageView_countryFlag"
                android:paddingLeft="@dimen/expanded_title_padding"
                android:paddingRight="@dimen/expanded_title_padding"
                android:paddingTop="@dimen/expanded_title_padding"
                tool:text="PLACE HOLDER"
                android:textColor="@color/expanded_title"
                android:textSize="@dimen/expanded_title" />

            <TextView
                android:id="@+id/tv_countryCode"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@id/tv_countryName"
                tool:text="PLACE HOLDER"
                android:paddingBottom="@dimen/country_code_padding_bottom"
                android:paddingLeft="@dimen/expanded_title_padding"
                android:paddingRight="@dimen/expanded_title_padding"
                android:textSize="@dimen/country_code_text_size" />

        </RelativeLayout>

    </android.support.v7.widget.CardView>

</LinearLayout>